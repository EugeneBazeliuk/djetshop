# ===================================
#  List Column Definitions
# ===================================

columns:

    id:
        label: ID
        searchable: true

    payment_total:
        label: djetson.shop::lang.list.payment_cost
        type: price
        width: 120px

    shipping_total:
        label: djetson.shop::lang.list.shipping_cost
        type: price
        width: 120px

    sub_total:
        label: djetson.shop::lang.list.subtotal
        type: price
        width: 120px

    total:
        label: djetson.shop::lang.list.total
        type: price
        width: 120px

    is_closed:
        label: djetson.shop::lang.form.is_closed
        type: switch_colored
        width: 1%

    is_paid:
        label: djetson.shop::lang.form.is_paid
        type: switch_colored
        width: 1%